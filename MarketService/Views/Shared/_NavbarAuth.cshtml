@if (User.Identity?.IsAuthenticated == true)
{
    <a href="/Chat" class="nav-btn" title="Tin nhắn" style="position: relative; border-radius: 50px; background: rgba(0,0,0,0.05); color: var(--text-main);">
        💬 <span class="badge bg-transparent text-dark p-0" style="font-weight: 600;">Chat</span>
        <span id="global-unread-badge" class="unread-count-dot" style="display: none;">0</span>
    </a>

    <a href="/Market/Cart" class="nav-btn" title="Giỏ hàng" style="position: relative; border-radius: 50px; background: rgba(0,0,0,0.05); color: var(--text-main);">
        🛒 <span class="badge bg-transparent text-dark p-0" style="font-weight: 600;">Giỏ hàng</span>
    </a>

    
    <div class="user-dropdown">
        <div class="user-avatar">
            @(User.Identity.Name?.Substring(0, 1).ToUpper() ?? "U")
        </div>
        <div style="font-size: 0.9rem; font-weight: 600; color: var(--dark);">
            @User.Identity.Name
        </div>
        <i class="bi bi-chevron-down ms-1 small text-muted"></i>

        <!-- Dropdown Menu Custom -->
        <div class="dropdown-menu-custom">
            <a href="/Profile" class="dropdown-item-custom">
                👤 Profile
            </a>
            <a href="/Order/MyOrders" class="dropdown-item-custom">
                📦 Đơn hàng của tôi
            </a>
            <a href="/Seller/Dashboard" class="dropdown-item-custom">
                🏪 Kênh người bán
            </a>
            @if (User.IsInRole("Admin"))
            {
                <a href="/Admin" class="dropdown-item-custom">
                    🛡️ Trang quản trị
                </a>
            }

            <hr class="my-1 opacity-10">
            <form asp-controller="Account" asp-action="Logout" method="post" id="logoutForm">
                <button type="submit" class="dropdown-item-custom text-danger border-0 bg-transparent w-100 text-start">
                    🚪 Đăng xuất
                </button>
            </form>
        </div>
    </div>
}
else
{
    <a href="/Account/Login" class="btn btn-outline-primary rounded-pill px-4">Đăng nhập</a>
    <a href="/Account/Register" class="btn btn-primary rounded-pill px-4">Đăng ký</a>

}
