<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - MarketService</title>
    <meta name="description" content="Hệ thống mua bán & Chat bảo mật" />

    <!-- Fonts hiện đại -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">

    <!-- CSS chính -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/buttons-forms.css" asp-append-version="true" />
    @await RenderSectionAsync("Styles", required: false)

    <!-- SignalR cho Chat -->
    <script src="https://cdn.jsdelivr.net/npm/@@microsoft/signalr@8.0.0/dist/browser/signalr.min.js"></script>
</head>
<body>
    <!-- Navbar Glassmorphism -->
    <partial name="_Navbar" />

    <!-- Menu Mobile (Ẩn mặc định) -->
    <div class="mobile-menu" id="mobile-menu">
        <a href="/" class="nav-link">Trang chủ</a>
        <a href="/Market" class="nav-link">Sản phẩm</a>
        <a href="/Post" class="nav-link">Cộng đồng</a>
        <hr style="border-top: 1px solid rgba(255,255,255,0.1); margin: 10px 0;">
        @if (User.Identity?.IsAuthenticated == true)
        {
            <a href="/Profile" class="nav-link">👤 Trang cá nhân (@User.Identity.Name)</a>
            <a href="/Order/MyOrders" class="nav-link">📦 Đơn hàng của tôi</a>
            <a href="/Seller/Dashboard" class="nav-link">🏪 Kênh người bán</a>
            @if (User.IsInRole("Admin"))
            {
                 <a href="/Admin" class="nav-link">🛡️ Trang quản trị</a>
            }
            <a href="/Market/Cart" class="nav-link">🛒 Giỏ hàng</a>

            <a href="/Chat" class="nav-link" style="position: relative; display: inline-block;">
                💬 Tin nhắn
                <span id="global-unread-badge-mobile" class="unread-count-dot" style="display: none; position: relative; top: 0; right: 0; margin-left: 5px;">0</span>
            </a>
            <form asp-controller="Account" asp-action="Logout" method="post" class="d-block mt-2">
                <button type="submit" class="btn btn-danger w-100 text-start py-2 px-3 rounded-pill" style="cursor:pointer">
                    🚪 Đăng xuất
                </button>
            </form>
        }
        else
        {
            <a href="/Account/Login" class="nav-link">Đăng nhập</a>
            <a href="/Account/Register" class="nav-link">Đăng ký</a>
            }
    </div>

    <!-- Nội dung chính -->
    <main class="main-content">
        @RenderBody()
    </main>

    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <p>&copy; 2025 MarketService - Hệ thống Bảo mật & Tiện lợi</p>
        </div>
    </footer>

    <!-- Nơi chứa Toast Notification -->
    <div id="toast-container"></div>

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>